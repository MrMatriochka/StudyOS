package org.example.studyos.contenu.domaine;

/** Nature pedagogique d'un document (modifiable a la main via PATCH). */
public enum TypeDocument {
    SLIDES,
    TP,
    ANNALE,
    NOTES,
    AUTRE
}
